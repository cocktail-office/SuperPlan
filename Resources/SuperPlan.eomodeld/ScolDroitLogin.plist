{
    attributes = (
        {
            columnName = "DLOG_KEY"; 
            externalType = NUMBER; 
            name = dlogKey; 
            precision = 6; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = "DLOG_LOGIN"; 
            externalType = VARCHAR2; 
            name = dlogLogin; 
            valueClassName = NSString; 
            valueType = S; 
            width = 20; 
        }, 
        {
            allowsNull = Y; 
            columnName = "DLOG_NOM"; 
            externalType = VARCHAR2; 
            name = dlogNom; 
            valueClassName = NSString; 
            valueType = S; 
            width = 80; 
        }, 
        {
            allowsNull = Y; 
            columnName = "DLOG_PRENOM"; 
            externalType = VARCHAR2; 
            name = dlogPrenom; 
            valueClassName = NSString; 
            valueType = S; 
            width = 60; 
        }, 
        {
            columnName = "DLOG_VALIDE"; 
            externalType = VARCHAR2; 
            name = dlogValide; 
            valueClassName = NSString; 
            valueType = S; 
            width = 1; 
        }
    ); 
    attributesUsedForLocking = (dlogKey, dlogLogin, dlogNom, dlogPrenom, dlogValide); 
    className = "org.cocktail.superplan.server.metier.ScolDroitLogin"; 
    classProperties = (dlogKey, dlogLogin, dlogNom, dlogPrenom, dlogValide); 
    externalName = "SCOLARITE.SCOL_DROIT_LOGIN"; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {
        "_clientClassPropertyNames" = (dlogKey, dlogLogin, dlogNom, dlogPrenom, dlogValide); 
        "_javaClientClassName" = "org.cocktail.superplan.client.metier.ScolDroitLogin"; 
    }; 
    name = ScolDroitLogin; 
    primaryKeyAttributes = (dlogKey); 
    userInfo = {fetchSpecificationDictionary = {}; }; 
}
